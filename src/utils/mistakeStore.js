const KEY='pwl-quiz-mistakes:v1';function read(){try{return JSON.parse(window.localStorage.getItem(KEY))||{entries:{}}}catch{return{entries:{}}}}function write(x){window.localStorage.setItem(KEY,JSON.stringify(x));return x}export function recordMistake({packageId,questionId,questionType,result}){const s=read(),k=`${packageId}::${questionId}`,old=s.entries[k]||{packageId,questionId,questionType,mistakeCount:0};const failed=questionType==='freeText'?result!=='green':!result; s.entries[k]={...old,lastResult:result,lastSeenAt:new Date().toISOString(),resolved:!failed,mistakeCount:failed?old.mistakeCount+1:old.mistakeCount};return write(s)}export function openMistakes(){return Object.values(read().entries).filter(x=>!x.resolved)}export function mistakeSummary(){const x=openMistakes();return{total:x.length,mc:x.filter(e=>e.questionType==='mc').length,yesNo:x.filter(e=>e.questionType==='yesNo').length,freeText:x.filter(e=>e.questionType==='freeText').length,packages:new Set(x.map(e=>e.packageId))}}export function createMistakeRound(size='all'){return openMistakes().sort((a,b)=>b.mistakeCount-a.mistakeCount||String(a.lastSeenAt).localeCompare(String(b.lastSeenAt))).slice(0,size==='all'?undefined:Number(size)).map(({packageId,questionId})=>({packageId,questionId}))}export function loadMistakeSession(){try{return JSON.parse(window.localStorage.getItem('pwl-quiz-mistake-session:v1'))||null}catch{return null}}export function saveMistakeSession(x){window.localStorage.setItem('pwl-quiz-mistake-session:v1',JSON.stringify(x))}